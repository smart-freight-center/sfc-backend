/*  Decided to add this file to ensure that unit test scripts work
    Should be deleted after we add the first feature or unit tests
    
    The entire 'example' module should be deleted
*/
export const someUsecase = (someId: string) => {
  return someId + ' ' + 50;
};
